<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">

        <activity android:name=".Task6"
            android:label="@string/task6Name">
        </activity>

        <activity android:name=".Task5"
            android:label="@string/task5Name">
        </activity>

        <activity android:name=".Task4"
            android:label="@string/task4Name">
        </activity>

        <activity android:name=".Task3"
            android:label="@string/task3Name">
        </activity>

        <activity android:name=".Task2"
            android:label="@string/task2Name">
        </activity>

        <activity android:name=".Task1"
            android:label="@string/task1Name">
        </activity>

        <activity android:name=".StartScreen"
        android:label="@string/startScreenName"
        android:parentActivityName=".MainActivity">
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value=".MainActivity"></meta-data>
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>